export enum TripStatus {
  Created = 'created',
  Waiting = 'waiting',
  Running = 'running',
  Finish = 'finish',
  Cancel = 'cancel'
}

export enum UserRoles {
  PASSENGER = 'USER',
  DRIVER = 'DRIVER'
}
